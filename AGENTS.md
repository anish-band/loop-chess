Commit after each working feature, not in bulk.
Commit messages: feat:, fix:, chore: prefixes only.
Never delete working code to make a check pass.
If stockfish.js WASM fails to load, surface the error in UI — don't silently swallow it.
Use functional React components and hooks only.
No TypeScript — plain JS.
Do not add co-author attribution to any commit message.

